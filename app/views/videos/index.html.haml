.row
  .twelve.columns
    .panel
      %h2#page-title Browse Video Tags
      #dynacloud
      
- @video_tags.each do |tag|
  .row{ :id => tag.name }
    .twelve.columns
      .panel
        %h3= tag.name
        
        - Video.tagged_with(tag.name).each do |video|
          .youtube-video
            %object
              %param{:name => "movie", :value => "http://www.youtube.com/v/#{video.youtube_url}&hl=en"}
              %param{:name => "wmode", :value => "transparent"}
              %embed{:src => "http://www.youtube.com/v/#{video.youtube_url}&hl=en", :id => "youtube-player", :type => "application/x-shockwave-flash", :wmode => "transparent"}
        
.dynacloud#dynatags
  - Video.tag_counts_on(:tags).each do |tag|
    = (tag.name + " ") * tag.count

#toTop ^ Back to Top