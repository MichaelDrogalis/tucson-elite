.row
  .panel#post-container
    .row
      .twelve.columns
        %h2#post-title= @post.title
    .row
      .twelve.columns
        = format(@post.content)
        = tweet_button
        = facebook_like
        
        - if user_signed_in? 
          = link_to 'Edit', edit_post_path(@post)
          \|
          = link_to 'Back', posts_path

#toTop ^ Back to Top
